/*$(document).ready(function() {
    $('.sendForm').submit(function() {
      var postText = $('.userPostText').val();
      var pictureData = $('.userPicBtn').prop('files');

      $.post('ajax/dashboardInsertPostToDatabase.php', {userPostText:postText, userPicBtn:pictureData}, function(data) {
        $('.postStatus').html(data);
        $('.userPostText').val('');

      });
    });
});*/
